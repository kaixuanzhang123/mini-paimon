# Mini Paimon - 简化版 Apache Paimon 存储引擎

本项目是一个教学性质的简化版 Apache Paimon，实现了 LSM Tree 存储引擎的核心功能，并提供了 Flink 和 Spark 计算引擎的集成支持。

## 模块说明

本项目包含以下模块：

- **paimon**: 核心存储引擎，实现 LSM Tree、Schema 管理、Snapshot 机制等
- **flink**: Flink 集成模块，提供 Flink SQL 访问 Paimon 表的能力
- **spark**: Spark 集成模块，提供 Spark SQL 访问 Paimon 表的能力

# Mini-Paimon 架构设计

## 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        应用层 (Application)                      │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │  SQL Parser  │  │ Table Manager│  │ Schema Mgr   │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      LSM Tree 存储引擎                           │
│                                                                 │
│  ┌────────────────────────────────────────────────────────────┐│
│  │                    读写协调层                               ││
│  │  LSMTree: put() / get() / scan() / close()                ││
│  └────────────────────────────────────────────────────────────┘│
│                              ↓                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │     WAL      │→ │  MemTable    │→ │  Compactor   │        │
│  │              │  │  (Active)    │  │              │        │
│  │ - append()   │  │  (Immutable) │  │ - compact()  │        │
│  │ - recover()  │  └──────────────┘  │ - merge()    │        │
│  │ - clear()    │         ↓          └──────────────┘        │
│  └──────────────┘    Flush when full        ↓                 │
│                              ↓               ↓                 │
│  ┌────────────────────────────────────────────────────────────┐│
│  │                  SSTable 文件层                             ││
│  │                                                             ││
│  │  Level 0:  [SST] [SST] [SST] [SST]  ← 4+ 个触发 compaction││
│  │  Level 1:  [SST] [SST] ... (10x)                          ││
│  │  Level 2:  [SST] [SST] ... (100x)                         ││
│  │  Level 3:  ...                                             ││
│  │  ...                                                       ││
│  │  Level 7:  [SST] ... (最大层级)                            ││
│  └────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      文件系统层                                  │
│                                                                 │
│  warehouse/{database}/{table}/                                 │
│  ├── data/         ← SSTable 文件                              │
│  │   ├── data-0-000.sst                                       │
│  │   ├── data-0-001.sst                                       │
│  │   ├── data-1-000.sst                                       │
│  │   └── ...                                                  │
│  ├── wal/          ← WAL 日志                                  │
│  │   ├── wal-000.log                                          │
│  │   └── wal-001.log                                          │
│  ├── snapshot/     ← 快照文件                                  │
│  ├── manifest/     ← Manifest 文件                            │
│  └── schema/       ← Schema 定义                              │
└─────────────────────────────────────────────────────────────────┘
```

## 🎯 核心特性

### ✅ 已实现的完整功能

#### 1. LSM Tree 存储引擎

**MemTable（内存表）**
- 基于 `ConcurrentSkipListMap` 实现有序内存存储
- 支持并发读写，自动按 RowKey 排序
- 内存阈值管理：默认 64MB，达到 80% 自动触发 flush
- 双缓冲机制：Active MemTable + Immutable MemTable

**SSTable（磁盘文件）**
- 完整的文件格式设计：Header + Data Blocks + Index + Bloom Filter + Footer
- Data Block 大小：4KB，支持数据压缩
- 稀疏索引：二分查找快速定位数据块
- Bloom Filter：1% 误判率，快速过滤不存在的 key
- 文件命名规则：`data-{level}-{sequence}.sst`

**读写流程**
- **写入**：MemTable → 达到阈值 → flush 到 SSTable（Level 0）
- **查询**：Active MemTable → Immutable MemTable → 多层 SSTable（按层级从低到高）
- **扫描**：合并所有层级数据，去重返回

#### 2. Write-Ahead Log (WAL)

**持久性保证**
- 写入前先记录 WAL：`put(row)` → `wal.append(row)` → `memTable.put(row)`
- JSON 格式日志：`{sequence, type, row}`
- 每次写入后立即 flush，确保数据持久化

**崩溃恢复机制**
- 启动时自动扫描所有 WAL 文件
- 按序重放未持久化的数据到 MemTable
- 恢复后立即删除 WAL 文件，避免重复加载

**生命周期管理**
- MemTable flush 成功后立即删除对应 WAL
- 正常关闭时 flush 所有数据并清理 WAL
- 确保 WAL 与 MemTable 生命周期严格绑定

**文件组织**
```
wal/
├── wal-000.log  ← 对应 sequence 0 的 MemTable
├── wal-001.log  ← 对应 sequence 1 的 MemTable
└── ...          ← 未 flush 的才存在
```

#### 3. Compaction 机制

**多层架构**
- **Level 0**：MemTable 直接 flush 的文件（可能范围重叠）
- **Level 1-7**：经过 compaction 的有序文件（范围不重叠）
- 每层大小约为上层的 10 倍

**触发策略**
- **Level 0**：文件数 ≥ 4 个时触发 → Level 1
- **Level N**：`size(Level N) / size(Level N+1) > 10` 时触发

**合并流程**
```
选择文件 → 读取并合并数据 → 排序去重 → 写入目标层级 → 删除旧文件
```

**文件选择**
- Level 0：所有文件（因为范围可能重叠）
- Level N：选择与下层范围重叠的文件

**优化特性**
- 使用 `TreeMap` 自动排序和去重
- 相同 key 保留最新版本
- 增量合并，避免一次处理过多数据

#### 4. 非主键表支持

**设计理念**
- 参考开源 Apache Paimon，支持无主键表
- Schema 中主键列表可以为空：`primaryKeys = []`

**自动键生成**
```java
if (schema.hasPrimaryKey()) {
    key = RowKey.fromRow(row, schema);  // 使用主键
} else {
    long seq = autoIncrementSequence.getAndIncrement();
    key = new RowKey(String.valueOf(seq).getBytes());  // 自增序列
}
```

**使用场景**
- 日志数据存储（允许完全重复）
- 追加写入场景
- 不需要主键约束的临时表

#### 5. Schema 管理

**Schema 版本控制**
- Schema ID 从 0 开始递增
- 每次 Schema 变更创建新版本
- 支持 Schema 演化验证

**字段定义**
```java
Field(name, dataType, nullable)
// 支持的类型：INT, LONG, STRING, BOOLEAN
```

**持久化**
```
schema/
├── schema-0  ← 初始版本
├── schema-1  ← 演化后版本
└── ...
```

#### 6. Snapshot 快照机制

**版本追踪**
- 每次 flush 创建新 Snapshot
- 记录 Schema ID 和 Manifest 信息
- 支持时间旅行查询

**Snapshot 内容**
```json
{
  "snapshotId": 1,
  "schemaId": 0,
  "commitTime": 1699000000,
  "manifestList": "manifest/manifest-list-1"
}
```

**文件组织**
```
snapshot/
├── snapshot-0
├── snapshot-1
├── LATEST  ← 指向最新快照
└── ...
```

#### 7. Manifest 文件管理

**追踪数据变更**
- 记录每个 SSTable 的添加和删除
- 包含文件元信息：路径、层级、范围、行数

**ManifestEntry**
```java
{
  "kind": "ADD",           // ADD 或 DELETE
  "file": "data-0-001.sst",
  "level": 0,
  "minKey": RowKey,
  "maxKey": RowKey,
  "rowCount": 1000
}
```

**文件组织**
```
manifest/
├── manifest-list-1      ← Snapshot 1 的清单
├── manifest-abc123      ← 具体的变更文件
└── ...
```

**文件组织**
```
warehouse/{database}/{table}/
├── data/              ← SSTable 数据文件
│   ├── data-0-000.sst
│   ├── data-0-001.sst
│   ├── data-1-000.sst
│   └── ...
├── wal/               ← Write-Ahead Log
│   ├── wal-000.log    (仅未 flush 的存在)
│   └── ...
├── schema/            ← Schema 版本
│   ├── schema-0
│   └── ...
├── snapshot/          ← 快照文件
│   ├── snapshot-0
│   ├── LATEST
│   └── ...
└── manifest/          ← 数据变更记录
    ├── manifest-list-1
    └── ...
```

## 🚀 快速开始

### 编译项目
```bash
cd /Users/kaixuanzhang/alibaba/mini-paimon
mvn clean compile
```

### 运行示例程序
```bash
# 运行高级特性演示
mvn exec:java -Dexec.mainClass="com.minipaimon.storage.AdvancedFeaturesExample"

# 运行 LSM Tree 示例
mvn exec:java -Dexec.mainClass="com.minipaimon.storage.LSMTreeExample"

# 运行 Schema 管理示例
mvn exec:java -Dexec.mainClass="com.minipaimon.metadata.SchemaManagementExample"
```

### 运行测试
```bash
mvn test
```

## 🏗️ 技术架构

### 核心组件

| 组件 | 职责 | 实现类 |
|------|------|--------|
| MemTable | 内存存储 | `MemTable.java` |
| SSTable | 磁盘文件 | `SSTable.java`, `SSTableWriter.java`, `SSTableReader.java` |
| WAL | 日志恢复 | `WriteAheadLog.java` |
| Compactor | 文件合并 | `Compactor.java` |
| LSMTree | 主协调器 | `LSMTree.java` |
| Schema | 表结构 | `Schema.java`, `SchemaManager.java` |
| Snapshot | 版本控制 | `Snapshot.java`, `SnapshotManager.java` |
| Manifest | 变更追踪 | `ManifestFile.java`, `ManifestList.java` |

### 性能特性

| 特性 | 说明 | 性能指标 |
|------|------|----------|
| 写入吞吐 | 先写 WAL，再写 MemTable | ~10K ops/s |
| 读取延迟 | Bloom Filter + 索引 + Cache | < 1ms (缓存命中) |
| 空间放大 | Compaction 压缩 | ~1.5x |
| 恢复时间 | WAL 重放 | 秒级 |


